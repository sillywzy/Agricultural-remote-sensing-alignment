open-clip-torch==3.3.0
timm==1.0.9
# torch 请按设备安装其一（本机验证版本）：
# CPU:  torch==2.2.2+cpu  / torchvision==0.17.2+cpu
# GPU:  torch==2.2.2+cu121 / torchvision==0.17.2+cu121 (RTX 3060, CUDA 12.x 验证通过)
numpy==1.26.4
pillow>=10
tqdm
regex
ftfy
huggingface-hub
safetensors
